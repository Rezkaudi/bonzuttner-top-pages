
import Container from '@/shared-components/Container';

const Footer: React.FC = () => {

    return (
        <footer>
            <Container>
                footer
            </Container>
        </footer>

    );
};

export default Footer;